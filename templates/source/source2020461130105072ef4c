<?php
if (strtolower(basename($_SERVER['PHP_SELF']))
		== strtolower(basename(__FILE__))) {
	header('HTTP/1.0 403 Forbidden');
	die();
}

function get_form_delete(&$controller, &$columns, &$list, $parameters) {
    $columns = [
        'id',
        'idcsv'
    ];

    $list = array();
}
?>